# instant-boxes

## 🔴 Windows users will need to install Git tool: [HERE](https://git-scm.com/download/win)

**This project is to build minimal sandboxes for coding purposes in the following languages**

- React (Vite + TSX)
- TypeScript (With webpack)

## Setup

1. Change directory to the desired coding environment and run:
   i. `./start.sh`
2. Follow output instructions
